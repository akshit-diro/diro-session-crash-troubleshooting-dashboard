# Copy to .env.local and adjust as needed
BASIC_AUTH_USER=admin
BASIC_AUTH_PASS=change_me

MOCK_MODE=false

# In your .env.local file:
SESSION_API_BASE_URL=https://prod.dirolabs.com
SESSION_API_ENDPOINT=/Zuul-1.0/User-2.0/getlastclickedlink

# Graylog adapter configuration (optional)
# Set at least GRAYLOG_BASE_URL and one of GRAYLOG_TOKEN or GRAYLOG_USERNAME/GRAYLOG_PASSWORD
# Example: https://graylog.dirolabs.com:9000
GRAYLOG_BASE_URL=

# Use a personal access token (recommended)
GRAYLOG_TOKEN=

# Or use basic auth credentials
GRAYLOG_USERNAME=
GRAYLOG_PASSWORD=

# Optional tuning
# Comma-separated stream IDs to narrow search (empty = all streams)
GRAYLOG_STREAMS=
# Relative search window in minutes
GRAYLOG_RANGE_MINUTES=120
# Maximum messages to fetch
GRAYLOG_LIMIT=500
# Additional query terms to prefix, e.g. source:app-*
GRAYLOG_QUERY_PREFIX=

# For self-signed certs, you may need to disable TLS verification in dev
# NODE_TLS_REJECT_UNAUTHORIZED=0

# Enable verbose server-side logging for Graylog fetches (dev only)
# DEBUG_GRAYLOG=1
